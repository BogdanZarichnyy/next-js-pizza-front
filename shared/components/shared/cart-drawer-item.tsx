import { cn } from "../../lib/utils";

interface Props {
  className?: string;
}

export const CartDrawerItem: React.FC<Props> = ({ className }) => { // 10:56:10
  return (
    <div className={cn("", className)}>

    </div>
  );
}